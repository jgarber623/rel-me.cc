services:
  web:
    image: jgarber623/rel-me-cc-web
    build:
      context: .
      target: development
    container_name: rel-me-cc-web
    init: true
    entrypoint: sleep infinity
    ports:
      - "8080:8080"
    volumes:
      - type: bind
        source: .
        target: /usr/src/app
        consistency: delegated
      - type: volume
        target: /usr/src/dependencies

networks:
  default:
    name: rel-me-cc-network
